// Global object

// console.log(global);

/*
global.setTimeout(() => {
    console.log("in timeout");
    clearInterval(int); // stops the int interval after setTimeout completes execution
}, 3000);
*/

/*
const int = setInterval(() => {
    console.log("in the interval");  // gets executed every second 
}, 1000);
*/

/*
console.log(__dirname); // gets absolute path of current folder
console.log(__filename); // gets absolute path of current folder along with filename
*/

